RobeWhisper · 第 10 期验收（步骤 5）
========================================

结论：通过（PHASE10_ACCEPTANCE_PASS）

一、本期交付摘要
--------------------------------
1. 实验 JSON 带 run_snapshot（引擎/LLM/机器人版本与非机密配置）
2. 经验按 model_ref 分区；默认禁止跨臂；旧无标签文件兼容默认臂族
3. 一次演示包 zip（任务单摘要 + 轨迹首末 + 快照 + 人话说明，脱敏）
4. 网站可导出/下载演示包；浏览表显示引擎/模型

二、「未堵死 11–15」红线复查
--------------------------------
[x] 不改 TaskSpec / SimResult / ApiResult 主字段名（快照为旁路字段）
[x] 不拆 BaseRobot.execute_task / HTTP / ROS 送单语义
[x] 经验旧键可读；新键只增不毁
[x] 跨臂默认关；EXPERIENCE_ALLOW_CROSS_MODEL 仅调试
[x] 演示包脱敏，无 API Key
[x] 未把伪对照 RSR 写成唯一终局
[x] 未做 3D / 视觉 / 机械狗 / 多用户云 / 强制公网
[x] Pipeline 主链不变：NL → LLM → TaskSpec → 闸 → engine → robot → 落盘/经验

三、1–10 成熟产品清单（总规划第四节）
--------------------------------
[x] 全链路稳定
[x] 能力边界与诊断可读
[x] 安全闸可演示
[x] 实验可复现关键版本信息；经验不跨臂污染
[x] 真机默认关；RSR 文案区分伪对照
[x] 对外口径为自然语言试机平台
[x] 接触抓取/真机必联/3D/视觉标为 11–15

四、复验命令
--------------------------------
python -m core.smoke_phase10

五、下一步（11–15，后置）
--------------------------------
真机联调 → 真轨迹 RSR → 接触抓取 → 多步 → 可视化/部署加分。
部署收尾时一并提醒：云服务器公网地址 + Moonshot/Kimi 充值激活 API
（清单：core/后期完善提醒_11到15.txt）。
