# RobeWhisper · 聊天记录备份（2026-09-12 · 第9–10期收口与 API 尝试）

> 用途：新对话时先读本文件 + `core/最新备份说明_2026-09-12.md` + `core/总规划_不超过15期.md`，避免需求被稀释。  
> 非逐字全文；是可执行共识与决策摘要。

---

## 1. 最新网站
- **本机临时站：http://127.0.0.1:8501**
- 打开方式：对 AI 说「打开网站」/「给我最新网址」；或 `一键启动网站.bat`
- **不是**公网长期地址；公网与云部署放在第 15 期 / 后期完善

---

## 2. 用户约束（持续有效）
- 零基础优先；**Python + Streamlit**
- 注释与回复用**简体中文**
- 流程：每阶段先方案（≤5 步）→ 一步一验 → 自行勘误 → 再下一步
- 成熟体系优先（MuJoCo、LLM API、ROS2 预留）；不自研引擎、不训大模型
- 只做机械臂；物理接触抓取等集中后期
- 第一期口径=语言试机+虚实接口演示，不上岗话术

---

## 3. 本轮完成的大事

### 第 9 期 · 安全闸与拒识（已完成）
- 关节限位 clip/reject；Pipeline 执行前接入
- 仿真墙钟超时 → `ENGINE_TIMEOUT`
- 危险词拒识；胡说/危险/超限可演示；诊断文案
- 总验：`PHASE9_ACCEPTANCE_PASS`

### 第 10 期 · 可复现与经验标签（已完成）
- 实验 `run_snapshot`（版本/后端/非机密配置）
- 经验按 `model_ref` 分区，默认禁止跨臂
- 演示包 zip 导出（脱敏）
- 总验：`PHASE10_ACCEPTANCE_PASS`
- **1–10 成熟产品清单已全部勾选**

### 全量回归
- 曾跑 `core` 下全部 smoke：**44/44 OK**
- 网站用最新代码重启验证

### API / Kimi（尝试后改回 mock）
- 用户提供 Moonshot/Kimi 密钥；已写入本机 `.env`（勿提交 Git）
- 实测：密钥有效，但账号 **余额不足/暂停（429）**，无法稳定调用
- **用户要求改回 mock** 保证演示稳定
- **明确要求**：把「充值激活 API」放到 **11–15 后期完善**，与**云服务器部署一起提醒**
- 已落地文件：`core/后期完善提醒_11到15.txt`  
  并写入总规划第 15 期、阶段10验收、回归一页纸、勘误记录

---

## 4. 当前运行真相
| 项 | 状态 |
|---|---|
| LLM | **mock**（实际解析） |
| `.env` 中 API 意图 | 可后期改为 api；密钥已留 |
| 真机 | 关 |
| RSR | 默认关；伪对照≠真闭环 |
| 经验 | 按模型分区；旧无标签兼容默认臂族 |
| 安全闸 | 开；默认 clip |

---

## 5. 概念共识（答辩口径）
产品 = 翻译器 + 沙盘 + 工单 + 送单窗口 + 账本/经验 + 回流软件管。  
真机需实验室执行节点；（RSR）需真实 trajectory。  
轨迹多为关节角时间序列；「grab」在无物体场景下为示意姿势。  
经验库 ≠ RL。物理摩擦/阻尼 ≠ 抓取成败裁判（接触抓取=第13期）。

---

## 6. 下一步（未开始）
- 第 11 期起：真机联调 → 真轨迹 RSR → 接触抓取 → 多步 → 可视化/部署  
- 部署收尾时打开 `core/后期完善提醒_11到15.txt`：  
  **云部署 + Moonshot 充值激活 API**（建议换新密钥，旧钥曾出现在聊天中）

---

## 7. 关键路径
- 总规划：`core/总规划_不超过15期.md`
- 第9/10验收：`core/阶段9验收.txt`、`core/阶段10验收.txt`
- 开 API 步骤（参考）：`core/开API安全步骤.txt`
- 后期提醒：`core/后期完善提醒_11到15.txt`
- 演示：`core/演示剧本.txt`
