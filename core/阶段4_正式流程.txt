RobeWhisper · 阶段 4 正式流程（重走版）
========================================

说明：首次实现曾“一次做完”，与阶段0–3「小步→验收→勘误」不一致。
本文件起按旧流程重走；代码复用已有模块，但逐步单独验收。

可行性：高（成熟文件存储，不自研 RL）。

步骤（≤5）：
1. 实验数据导出（experiments/ JSON + summary.csv）
2. 经验库存取（experience/ 按 action+target）
3. Pipeline 接入：执行后落盘；成功经验可复用
4. 网站展示实验/经验，聊天回执含 experiment_id / experience_hit
5. 阶段4总验收 + 阶段1–3回归 + 勘误收口

禁止：改 TaskSpec 主字段；上 ROS；自训模型。
