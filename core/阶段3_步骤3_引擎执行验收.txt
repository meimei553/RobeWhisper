RobeWhisper · 阶段 3 · 步骤 3 验收
========================================

结论：通过（SMOKE_PHASE3_STEP3_OK）

交付：
- core/pipeline.py：自然语言 → TaskSpec → Engine.run_task（默认 default 模型）
- 仿真失败/异常均返回友好 ApiResult，不抛堆栈

验证：
- 「把桌上的红色杯子拿起来」+ MuJoCo → grab/red_cup，轨迹运动
- 非法指令 REJECTED；ENGINE_BACKEND=mock 仍可用

复验：python -m core.smoke_phase3_step3
