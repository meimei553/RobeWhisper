RobeWhisper · 阶段 1 总验收（步骤 5）
========================================

验收日期：2026-09-11
结论：阶段 1 通过（PHASE1_ACCEPTANCE_PASS）

一、验收结果
--------------------------------
[x] MuJoCo 已安装可选，适配器 MujocoEngine 可用
[x] 默认模型 models/builtin_arm2/model.xml 经 MODELS_DIR 加载
[x] 命令行/同款 API 下关节角一步运动可见
[x] 临时网站 http://127.0.0.1:8501 可打开，含滑条步进与状态显示
[x] 页面不直接 import mujoco；pipeline 不绑定具体引擎类
[x] ENGINE_BACKEND=mock 可回退；契约主字段未改
[x] 未混入 LLM 聊天主流程 / 经验库 / ROS 重依赖

一句话确认（进入阶段 2 的前提）：
阶段 2 只加「模型导入 + 物理参数面板」，不改 BaseEngine 方法名与 TaskSpec 主字段。


二、阶段 2 允许 / 禁止
--------------------------------
允许：
- 上传/选择 URDF 或 MJCF（引擎支持的格式）
- 参数面板：质量、摩擦、关节限位等（改引擎已暴露字段）
- 简单接触/掉落观察

禁止：
- 修改 TaskSpec / SimResult / ApiResult 主字段名
- 自研物理核或精细柔性体
- 上线云端 LLM 聊天主流程（阶段 3）
- 经验库 / Real-Sim-Real / 真机 ROS（更后）


三、关于「最终网站地址」
--------------------------------
- 现在：只有本机临时地址 http://127.0.0.1:8501（需本机运行 Streamlit）
- 以后：把同一套产品部署到云服务器/固定域名后，会得到可分享的恒定网址
- 功能：那个恒定网址承载的是「同一产品」——随阶段 2/3/4…把导入、对话、实验、真机口逐步做进这个网站，而不是另做一堆互不相通的站


四、复验命令
--------------------------------
python -m core.smoke_mujoco_step3
python -m core.smoke_phase1
streamlit run frontend/app.py --server.address 127.0.0.1 --server.port 8501
