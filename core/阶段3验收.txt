RobeWhisper · 阶段 3 总验收（步骤 5）
========================================

验收日期：2026-09-11
结论：阶段 3 通过（PHASE3_ACCEPTANCE_PASS）

一、本步修复
--------------------------------
- 页面报错 DEFAULT_MODEL_REL：已确认 settings 含该字段；
  model_catalog 改为 getattr 容错；并重启 Streamlit 清热重载脏状态。


二、验收结果
--------------------------------
[x] LLM 可插拔（mock|api，无 Key 回退 mock）
[x] System Prompt + JSON/白名单校验
[x] Pipeline：自然语言 → TaskSpec → 引擎执行
[x] 网站聊天区 + session_state 历史；保留模型/参数/关节区
[x] 契约主字段未改；页面不直连 mujoco/厂商 SDK
[x] 阶段 0–2 回归可跑通


三、阶段 4 允许 / 禁止（下一步）
--------------------------------
允许：虚拟实验数据导出、轻量经验库（成功轨迹入库/回放）
禁止：改契约主字段、自训大模型、ROS 真机主线、重做物理核


四、入口
--------------------------------
http://127.0.0.1:8501
试一句：把桌上的红色杯子拿起来

复验：python -m core.smoke_phase3
